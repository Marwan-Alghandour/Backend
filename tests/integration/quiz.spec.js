const { Course } = require("../../src/resources/courses/course.model");
const { Quiz } = require("../../src/resources/quizes/quiz.model");

describe("Quiz unit testing", () => {
    it("should return grade of the quiz", () => {
        expect("a").toBe("a");
    });
});

